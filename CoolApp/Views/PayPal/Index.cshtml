@model CoolApp.Models.PayPal.Payment

@{
    Layout = null;
}

@using(Html.BeginForm("Pay","PayPal", FormMethod.Post))
{
    @Html.HiddenFor(model => model.SecundaryReceiver)
    @Html.HiddenFor(model => model.Amount)
    @Html.HiddenFor(model => model.Fee)
    @Html.HiddenFor(model => model.PrimaryReceiver)
    @Html.HiddenFor(model => model.ReturnCancelURL)
    @Html.HiddenFor(model => model.ReturnURL)
    @Html.HiddenFor(model => model.PreapprovalKey)
    <input type="submit" value="BUY"/>
}

@using(Html.BeginForm("PayPreapproved","PayPal", FormMethod.Post))
{
    @Html.HiddenFor(model => model.SecundaryReceiver)
    @Html.HiddenFor(model => model.Amount)
    @Html.HiddenFor(model => model.Fee)
    @Html.HiddenFor(model => model.PrimaryReceiver)
    @Html.HiddenFor(model => model.ReturnCancelURL)
    @Html.HiddenFor(model => model.ReturnURL)
    @Html.HiddenFor(model => model.PreapprovalKey)
    <input type="submit" value="BUY PREAPPROVED"/>
}

@using(Html.BeginForm("ConfigurePreappoval","PayPal", FormMethod.Post))
{
    @Html.HiddenFor(model => model.ReturnCancelURL)
    @Html.HiddenFor(model => model.ReturnURL)
    <input type="submit" value="Configure Preapproval"/>
}